`n_jobs` in {func}`scanpy.pp.regress_out` now selects Numba threads rather than `joblib` processes, so it also caps the threads used by the vectorized path — which previously ignored it and used every core. A gene the covariates explain exactly is no longer rounded to zeros with an `Encountered perfect separation` warning: that behavior existed to swallow a `statsmodels` warning ({pr}`3275`), and `statsmodels` is no longer used {smaller}`S Ozleyen`
