Create {mod}`scanpy.io` and move `scanpy.{read*,write*}` and `scanpy.external.exporting.*` into it. The old names are deprecated, and `scanpy.read_loom` now warns about its upcoming removal {smaller}`P Angerer`
