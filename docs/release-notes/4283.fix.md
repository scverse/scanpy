Fix several correctness problems in {func}`scanpy.pp.regress_out`: solve through the pseudo-inverse of a centered design instead of `inv(RᵀR)`, which squared the condition number and returned residuals wrong by more than their own standard deviation for a barely-varying covariate; compute category means in `float64` rather than through a `float32` regressor matrix; preserve the input dtype rather than upcasting to `float64` on some designs; leave zero-variance genes untouched on every design rather than only some; sum duplicate entries of a non-canonical sparse matrix; raise on a non-finite covariate instead of returning an all-`NaN` matrix; accept `bool` and nullable-integer covariates; accept a {class}`~dask.array.Array` on every design rather than just one; and stop `n_jobs=-1` and `n_jobs=0` from raising {smaller}`S Ozleyen`
