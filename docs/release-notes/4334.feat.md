Add a `use` parameter taking {mod}`anndata.acc` accessors (e.g. `A.layers["counts"]`) to the functions selecting an input matrix, deprecating their `layer`, `obsm` and `use_rep` parameters.

Same-shape transforms – {func}`~scanpy.pp.scale`, {func}`~scanpy.pp.log1p`, {func}`~scanpy.pp.regress_out`, {func}`~scanpy.pp.normalize_total` – and {func}`~scanpy.tl.score_genes` additionally gain an `out` parameter saying where to write the result:
pass an accessor to write there, or `out=None` to get the result returned instead (replacing `inplace=False` and `score_name`) {smaller}`P Angerer`
