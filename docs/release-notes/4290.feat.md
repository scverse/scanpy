Derive unset {func}`scanpy.pp.harmony_integrate` stopping rules from `flavor`, speeding up `harmony2` considerably: it now follows the Harmony2 defaults, while `harmony1` still follows harmony-pytorch {smaller}`S Dicks`
