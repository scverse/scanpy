perf(score_genes): avoid copy-heavy sparse nan mean path

The `_sparse_nanmean` function now aggregates sums and NaN counts directly via compressed index pointers (CSR/CSC) instead of making sparse matrix copies and calling `eliminate_zeros()`. This significantly improves performance for sparse matrices with NaN values.

Also adds test coverage for both CSR and CSC sparse formats.