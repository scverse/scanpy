Rewrite {func}`scanpy.pp.regress_out` around Numba threads and {class}`~scanpy.get.aggregate`’s group-sum kernels instead of `joblib` processes calling `statsmodels`, making it 1–2 orders of magnitude faster and no longer oversubscribing CPUs by nesting BLAS threads inside worker processes. `n_jobs` is now a Numba thread count and results are bitwise identical for every value of it. Regressing on a categorical key no longer materializes an `n_obs` × `n_vars` regressor matrix {smaller}`S Ozleyen`
